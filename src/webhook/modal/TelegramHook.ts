import BaseRactive, { BaseRactiveInterface } from "base/BaseRactive";

const TelegramHook = BaseRactive.extend<BaseRactiveInterface>({
  template:/* html */`
    <div class="modal-body">
          
    </div>
  `
});

export default TelegramHook;