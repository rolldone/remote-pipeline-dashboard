import BaseRactive, { BaseRactiveInterface } from "base/BaseRactive";

const SlackHook = BaseRactive.extend<BaseRactiveInterface>({
  template:/* html */`
    <div class="modal-body">
          
    </div>
  `
});

export default SlackHook;